default_profile: lgbm_default

profiles:
  lgbm_default:
    path: configs/models/lgbm_default.yaml

  lgbm_default_rankic:
    path: configs/models/lgbm_default_rankic.yaml

  lgbm_fast:
    path: configs/models/lgbm_fast.yaml

  lgbm_cuda_fast:
    path: configs/models/lgbm_cuda_fast.yaml

  lgbm_regularized:
    path: configs/models/lgbm_regularized.yaml

  lgbm_binary_default:
    path: configs/models/lgbm_binary_default.yaml

  lgbm_ranker_default:
    path: configs/models/lgbm_ranker_default.yaml

  lgbm_ranker_topk15_excess:
    path: configs/models/lgbm_ranker_topk15_excess.yaml

  lgbm_ranker_rankic:
    path: configs/models/lgbm_ranker_rankic.yaml

  formula_score_rank_avg:
    path: configs/models/formula_score_rank_avg.yaml

  formula_score_sign_aligned:
    path: configs/models/formula_score_sign_aligned.yaml

  formula_score_rankic:
    path: configs/models/formula_score_rankic.yaml

  lstm_baseline:
    path: configs/models/lstm_baseline.yaml
